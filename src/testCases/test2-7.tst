main
var a, b, c;

function f;
{
  let a <- a + 1;
  return a
};

function g;
{
  let b <- b + 1;
  return b
};

{
  let a <- 1;
  let b <- 2;
  let c <- 3;
  while c < 10 do
    if call f < call g then
      call outputnum(c)
    fi;
    let c <- c + 1
  od
}.
