main
var a, b, c, d;
{
  let a <- 424;
  let b <- 4920;
  let c <- 9302;
  let d <- 2391;

  if a != b then
    if b != c then
      if c != d then
        call outputnum(a)
      else
        call outputnum(a)
      fi
    else
     call outputnum(a)
    fi
  else
    call outputnum(a)
  fi;
  call outputnewline()
}.
